@using Periodicals.Models
@*model TopicModel*@


<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Topics<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var item in TopicModel.Topics)
        {
        <li>@Html.ActionLink(item.TopicName, "Topics", "Home", new { area = "", topicId=item.Id }, null)</li>
        }
    </ul>
</li>